id: function
utils:
  is_funcdef:
    kind: identifier
    inside:
      kind: function_definition

language: python
rule:
    any:
      - inside:
          kind: decorated_definition
          stopBy: end
        matches: is_funcdef
      - matches: is_funcdef
